{
  "name": "4d-bank-server",
  "main": "index.js",
  "private": true,

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "dependencies": {
    "express": "^4.15",
    "mongoose": "^4.11",
    "uuid": "^3.1"
  }
}
